# Telegram VPN Billing Bot

Асинхронный Telegram-бот на **aiogram 3** для коммерческого VPN-сервиса на базе VLESS/REALITY с интеграцией панели Marzban и платёжного провайдера.

## Возможности
- Приветствие, компактное меню и онбординг.
- Покупка/продление подписок (1/3/6 месяцев) с генерацией инвойсов и idempotency на вебхуках оплат.
- Авто-создание или продление пользователя в Marzban, получение personal subscription link.
- Интерактивные инструкции установки для Android/iOS/Windows/macOS.
- Статус подписки и продление в один клик.
- Реферальная ссылка с бонусными днями за каждого приглашённого.
- Пробный период на 1 день с отдельным лимитом трафика.
- FAQ/Help и условия оферты.

## Быстрый старт
1. Установите зависимости:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```
2. Создайте файл `.env`:
   ```env
   TELEGRAM_TOKEN=123:bot-token
   TELEGRAM_ADMIN_IDS=123456
   MARZBAN_BASE_URL=https://panel.example.com
   MARZBAN_API_KEY=secret
   PAYMENT_PROVIDER_KEY=provider-key
   PAYMENT_PUBLIC_KEY=provider-pub
   PAYMENT_WEBHOOK_SECRET=webhook-secret
   PAYMENT_CURRENCY=USD
   DATABASE_PATH=./bot.db
   WEBHOOK_HOST=0.0.0.0
   WEBHOOK_PATH=/payment/webhook
   BASE_SUBSCRIPTION_DAYS=30
   REFERRAL_BONUS_DAYS=7
   ```
3. Запустите бота (polling + вебхук оплаты на `:8080`):
   ```bash
   python main.py
   ```
4. Настройте платёжного провайдера на отправку webhook-запросов по адресу `http(s)://<host>:8080/payment/webhook` c заголовком `X-Signature` (HMAC-SHA256 тела запроса).

## Архитектура
- `app/services` — интеграция с Marzban, платежами, рефералами, подписками, DI middleware.
- `app/repositories` — хранение пользователей, платежей и рефералов в SQLite (aiosqlite), атомарные операции для idempotency.
- `app/handlers` — Telegram UI: старт, покупка, продление, статус, установка, реферальная программа, помощь.
- `app/server.py` — aiohttp-приложение для приёма вебхуков платежей.
- `app/config.py` — pydantic-настройки и тарифы (1/3/6 месяцев).
- `main.py` — сборка зависимостей и запуск бота + вебхука.

## Безопасность и эксплуатация
- Все токены и ключи берутся из ENV, секреты не хранятся в коде.
- Вебхук оплат верифицирует подпись HMAC (idempotent обработка через `payments` таблицу).
- Повторные оплаты/дубль-вебхуки не создадут дублирующих учёток.
- Для каждого Telegram ID создаётся единый пользователь в Marzban (`tg_<id>`), продление проходит через API `/renew`.

## Типовые сообщения
- `/start`: «👋 Добро пожаловать! Мы даём быстрый и стабильный VPN…» + меню с кнопками.
- «Купить VPN»: выбор тарифа с ценой → ссылка на оплату.
- «Установить VPN»: выбор платформы → 3–4 шага подключения и ссылка на приложение.
- «Статус»: дата окончания, лимит трафика и кнопка «Продлить».
- «Пробный период»: 1 день доступа и 5 GB трафика (один раз на пользователя).
- «Пригласить друга»: выдаёт персональную ссылку вида `https://t.me/<bot>?start=<id>`.
- «Помощь»: быстрый FAQ и ссылка на поддержку.

## Trial & Referrals
- Пробный период активируется один раз на пользователя: 1 день и 5 GB трафика.
- Повторное нажатие Trial возвращает сообщение об already-used состоянии.
- Реферальная ссылка ведёт на `/start <referrer_id>`, self-referral запрещён.
- Бонус рефереру (+7 дней) начисляется только после оплаты приглашённого и только один раз.

## Подготовка к production
- Разверните `main.py` за reverse-proxy (HTTPS) для корректной приёмки вебхуков оплат.
- Включите мониторинг и логирование (stdout подхватит systemd/Docker). При необходимости расширьте `logging` формат.
- Настройте бэкап `bot.db` и базы Marzban; вынесите БД в внешний PostgreSQL при росте нагрузки.
- Добавьте вендорский платёжный SDK в `PaymentService` (метод `create_invoice` и `verify_webhook`).
